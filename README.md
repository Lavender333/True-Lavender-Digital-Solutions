# True Lavender Digital Solutions

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies: `npm install`
2. Run the app: `npm run dev`
3. Build for publishing: `npm run build`

## Deploy

The production build is generated in the `dist` folder.
